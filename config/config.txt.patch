# Lines to add to /boot/firmware/config.txt for Spotifoni
# Keep onboard audio enabled so both outputs are available

# Enable I2S DAC (MAX98357A) — coexists with onboard headphone jack
dtoverlay=hifiberry-dac
